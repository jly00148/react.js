一、step：
npm i redux --save

二、另创建文件夹store里的index.js文件创建store：
1. import { createStore } from 'redux';

2. 创建store：
    const store = createStore(()=>{
        
    })
    export default store

3. 导出:
    export defalult store

4. App-store打印store